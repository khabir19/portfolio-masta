import { BrowserRouter } from 'react-router-dom';

import RouteBuild from './routes';

const Routes = () => (
  <>
    <BrowserRouter>
      <RouteBuild />
    </BrowserRouter>
  </>
);

export default Routes;
